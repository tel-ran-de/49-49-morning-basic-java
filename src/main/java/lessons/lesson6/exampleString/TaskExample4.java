package lessons.lesson6.exampleString;

public class TaskExample4 {
    public boolean isEven(int checkNumber){
        int resultDiv = (checkNumber % 2);

        boolean checkResult =  (resultDiv==0);

        return checkResult;
    }
}
